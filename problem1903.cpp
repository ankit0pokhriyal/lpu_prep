#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
bool isSorted(const vector<long long>& arr) {
    // if in ascending order return true 
    // other wise false

}

int main() {
  
    int t;
    cin >> t;
    while (t--) {
        int n;
        long long k;
        cin >> n >> k;
        vector<long long> arr(n);
        for (int i = 0; i < n; ++i) {
            cin >> arr[i];
        }
            
        // check if sorted then yes
        // check for k condition

    }
    return 0;
}