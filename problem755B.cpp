#include <iostream>
#include <vector>
#include <string>
#include <set>

using namespace std;
int main() {

    int n, m;
    cin >> n >> m;

   // create set for polandball
    set<string> polandball_words;

    set<string> enemyball_words;
    
    // count common word
    int common_words_count = 0;
    
    // count unique count for both
     
    // count common turns

    // count total moves
    

    // use condition for polandball_total_moves, enemyball_total_moves

    return 0;
}